# ============================================================
# Unraid Arr Stack - Environment Configuration
# ============================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# ============================================================
# GENERAL
# ============================================================
# Your user/group IDs (run: id $USER on Unraid terminal)
PUID=1000
PGID=1000

# Timezone (https://en.wikipedia.org/wiki/List_of_tz_database_time_zones)
TZ=Europe/Berlin

# Base path for all app config data (on Unraid: /mnt/user/appdata)
APPDATA=/mnt/user/appdata

# ============================================================
# MEDIA & DOWNLOAD PATHS
# ============================================================
# Root data folder — all media and downloads live here
# Unraid best practice: single share so hardlinks work across containers
DATA=/mnt/user/data

# Sub-paths (relative to DATA, created by setup.sh)
# ${DATA}/media/movies       -> finished movies
# ${DATA}/media/tv           -> finished TV shows
# ${DATA}/downloads/torrents -> torrent downloads (incomplete + complete)
# ${DATA}/downloads/usenet   -> usenet downloads (incomplete + complete)

# ============================================================
# VPN (Gluetun)
# ============================================================
# VPN provider name — see gluetun wiki for full list:
# https://github.com/qdm12/gluetun-wiki/tree/main/setup/providers
VPN_SERVICE_PROVIDER=mullvad

# VPN protocol: openvpn | wireguard
VPN_TYPE=wireguard

# --- WireGuard (fill if VPN_TYPE=wireguard) ---
WIREGUARD_PRIVATE_KEY=your_wireguard_private_key_here
WIREGUARD_ADDRESSES=10.64.0.1/32

# --- OpenVPN (fill if VPN_TYPE=openvpn) ---
OPENVPN_USER=your_vpn_username
OPENVPN_PASSWORD=your_vpn_password

# Optional: pin to specific server(s)
# SERVER_COUNTRIES=Netherlands
# SERVER_CITIES=Amsterdam

# ============================================================
# qBITTORRENT
# ============================================================
# Web UI password hash (default: adminadmin — change after first login!)
# Leave empty to use the default credentials on first start.
QBITTORRENT_WEBUI_PORT=8080

# ============================================================
# SABNZBD (Usenet)
# ============================================================
SABNZBD_PORT=8090

# ============================================================
# PROWLARR (Indexer Manager)
# ============================================================
PROWLARR_PORT=9696

# ============================================================
# RADARR (Movies)
# ============================================================
RADARR_PORT=7878

# ============================================================
# SONARR (TV Shows)
# ============================================================
SONARR_PORT=8989

# ============================================================
# LIDARR (Music) — set LIDARR_ENABLED=true to include
# ============================================================
LIDARR_ENABLED=false
LIDARR_PORT=8686

# ============================================================
# READARR (Books/AudioBooks) — set READARR_ENABLED=true to include
# ============================================================
READARR_ENABLED=false
READARR_PORT=8787

# ============================================================
# BAZARR (Subtitles)
# ============================================================
BAZARR_PORT=6767

# ============================================================
# OVERSEERR (Media Requests)
# ============================================================
OVERSEERR_PORT=5055

# ============================================================
# JELLYFIN (Media Server)
# ============================================================
JELLYFIN_PORT_HTTP=8096
JELLYFIN_PORT_HTTPS=8920

# Hardware transcoding device (leave empty to disable)
# For Intel QuickSync:   /dev/dri
# For Nvidia:            handled via runtime in compose file
# JELLYFIN_GPU_DEVICE=/dev/dri

# ============================================================
# HOMEPAGE (Dashboard)
# ============================================================
HOMEPAGE_PORT=3000
